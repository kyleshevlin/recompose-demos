import React from 'react'
import CollapsibleSection from '../CollapsibleSection'

const Pure = () => (
  <CollapsibleSection heading={'pure()'}>
    <div>
      <p>
        The <code>pure</code> higher order component returns a <code>PureComponent</code> version of
        your SFC.
      </p>

      <p>
        <span className="boom">💥</span>
      </p>

      <p>That's it.</p>
    </div>
  </CollapsibleSection>
)
export default Pure
